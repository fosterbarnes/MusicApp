<UserControl x:Class="MusicApp.Views.ArtistGenreView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:MusicApp.Views"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    <Grid>
        <views:TrackListView x:Name="trackList"
                             SingleClickPlays="True"
                             PlayTrackRequested="TrackList_PlayTrackRequested" />
        <TextBlock x:Name="placeholderArtists"
                   Text="No artists view yet :("
                   Foreground="#888888"
                   FontSize="16"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"/>
        <TextBlock x:Name="placeholderGenres"
                   Text="No genres view yet :("
                   Foreground="#888888"
                   FontSize="16"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Visibility="Collapsed"/>
    </Grid>
</UserControl>
