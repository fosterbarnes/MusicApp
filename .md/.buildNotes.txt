v0.1.6 release (crispyFartin)

Backend/Boring stuff:
- Move `/musicApp/Resources` to `/.resources`
- Move `/musicApp/Scripts` to `/.scripts`
- Move `/.web/mainPage/Scripts` to `/.scripts/web`

Build config:
- Add arm64 config
- Rename "AnyCPU" > "portable"

Installer:
- Add arm64 support

Scripts:
- Consolidate powershell script location
- Clean up script logic